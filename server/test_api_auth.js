const jwt = require('jsonwebtoken'); const http = require('http'); const token = jwt.sign({ id: 1, email: 'admin@gmail.com', role: 'ADMIN' }, 'secret'); console.log('Token:', token); const req = http.request('http://localhost:5000/api/projects/1/tasks/6', { method: 'PATCH', headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer ' + token } }, (res) => { let data = ''; res.on('data', chunk => data += chunk); res.on('end', () => console.log('Status:', res.statusCode, 'Body:', data)); }); req.write(JSON.stringify({ assignees: [1, 2] })); req.end();
